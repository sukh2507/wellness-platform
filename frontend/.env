# Frontend .env file
# Place this file in your React app root directory (same level as package.json)

# API Configuration
VITE_API_URL=http://localhost:5000

# Development settings
NODE_ENV=development

# Optional: Custom port for React dev server (uncomment if needed)
# PORT=3000

# Optional: Disable browser auto-open (uncomment if needed)
# BROWSER=none